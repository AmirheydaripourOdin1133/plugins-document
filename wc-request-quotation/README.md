# WooCommerce Request Quotation — رسام سرور

افزونهٔ اختصاصی ووکامرس برای درخواست **پیش‌فاکتور** از صفحهٔ محصول، تولید **PDF فارسی**، و مدیریت درخواست‌ها در پنل ادمین.

| | |
|---|---|
| **نسخه** | 2.4.4 |
| **نیازمند** | WordPress 6.0+ ، WooCommerce ، PHP 7.4+ |
| **نویسنده** | [Amir heydaripour](https://t.me/amir_drwp) |
| **دانشنامه** | [مستندات GitHub](https://github.com/AmirheydaripourOdin1133/plugins-document/tree/main/wc-request-quotation) |
| **سازگاری** | محصول ساده و متغیر ، افزونه `woo-variations-table-grid` ، یکپارچگی با `rasam-server-config` |

> در **افزونه‌ها → نصب‌شده**، کنار نام افزونه لینک **دانشنامه** به همین آدرس GitHub نمایش داده می‌شود.

---

## فهرست

1. [قابلیت‌ها](#قابلیت‌ها)
2. [نصب](#نصب)
3. [تنظیمات](#تنظیمات)
4. [جریان کار فرانت](#جریان-کار-فرانت)
5. [جریان کار ادمین](#جریان-کار-ادمین)
6. [ذخیره‌سازی PDF و دیتابیس](#ذخیره‌سازی-pdf-و-دیتابیس)
7. [ساختار فایل‌ها](#ساختار-فایل‌ها)
8. [متا فیلدها](#متا-فیلدها)
9. [یکپارچگی با Rasam Server Config](#یکپارچگی-با-rasam-server-config)
10. [عیب‌یابی](#عیب‌یابی)
11. [توسعه و نگهداری](#توسعه-و-نگهداری)
12. [تاریخچه نسخه‌ها](#تاریخچه-نسخه‌ها)
13. [کارهای پیشنهادی آینده](#کارهای-پیشنهادی-آینده)

---

## قابلیت‌ها

### فرانت (مشتری)

- دکمه **«درخواست پیش‌فاکتور»** در **صفحهٔ تک‌محصول** (ساده و متغیر)
- CSS/JS و پاپ‌آپ فقط روی `is_product()` لود می‌شوند (نه در آرشیو/صفحات دیگر)
- پاپ‌آپ فرم: نام، تماس، شرکت، کد ملی/اقتصادی، تعداد
- چک‌باکس **نیاز به تماس کارشناس** (متن قابل تنظیم در ادمین)
- تولید و دانلود PDF بلافاصله پس از ثبت
- پشتیبانی از جدول وریشن (`woo-variations-table-grid`): ستون **کانفیگ** فقط برای محصول متغیر در PDF

### ادمین

- نوع پست `quotation_request` — لیست درخواست‌ها با ستون‌های سفارشی
- متاباکس **اطلاعات پیش‌فاکتور** (جزئیات مشتری و محصول)
- متاباکس **مدیریت PDF** (دانلود، ساخت مجدد، حذف)
- متاباکس **پیگیری تماس** (وضعیت تماس + گزارش مختصر)
- رنگ‌بندی لیست: تماس جدید / در انتظار / تماس گرفته‌شده
- ویجت پیشخوان: درخواست‌های در انتظار تماس
- شمارنده در منوی ادمین (مشابه سفارش جدید)

---

## نصب

1. پوشهٔ `wc-request-quotation` را در `wp-content/plugins/` قرار دهید.
2. از **افزونه‌ها** افزونه را فعال کنید.
3. مطمئن شوید **WooCommerce** فعال است.
4. منوی **تنظیمات پیش‌فاکتور** را برای آدرس، مالیات، شبا و … تکمیل کنید.
5. پوشهٔ `wp-content/uploads/quotations/` باید قابل نوشتن باشد (خودکار ساخته می‌شود).

### وابستگی‌های پیشنهادی (سایت رسام)

| افزونه | نقش |
|--------|-----|
| WooCommerce | محصولات و قیمت |
| woo-variations-table-grid | جدول وریشن + دکمه پیش‌فاکتور per row |
| rasam-server-config | نمایش RTL جزئیات در ادمین + پیکربندی سفارشی |

---

## تنظیمات

مسیر: **تنظیمات پیش‌فاکتور** (منوی سایدبار وردپرس)

| فیلد | کاربرد |
|------|--------|
| آدرس / تلفن / کد پستی / کد اقتصادی | سربرگ PDF |
| URL وب‌سایت | سربرگ PDF |
| شماره شبا / کارت / نام بانک | بلوک پرداخت PDF |
| درصد مالیات | محاسبه مالیات در PDF |
| متن توضیحات فاکتور | پاورقی PDF (مثلاً اعتبار ۲۴ ساعته) |
| متن تیک «نیاز به تماس» | برچسب چک‌باکس در پاپ‌آپ |

---

## جریان کار فرانت

```
کلیک «درخواست پیش‌فاکتور»
    → پاپ‌آپ فرم
    → AJAX: wc_rq_submit_form
    → ایجاد quotation_request + متادیتا
    → generate_pdf_invoice()
    → باز شدن PDF در تب جدید
```

### محصول متغیر (جدول وریشن)

- `is_variable = 1` ارسال می‌شود
- نام محصول به صورت `عنوان (مشخصات وریشن)` ذخیره می‌شود
- در PDF ستون **کانفیگ** نمایش داده می‌شود

### محصول ساده

- `is_variable = 0`
- در PDF فقط ستون **نام محصول** (بدون کانفیگ)

---

## جریان کار ادمین

### لیست درخواست‌ها

`ویرایش → درخواست‌های پیش‌فاکتور`

| ستون | توضیح |
|------|--------|
| عنوان | نام پیش‌فاکتور + برچسب «نیاز به تماس» |
| نام مشتری / شماره تماس | از متادیتا |
| تماس | badge رنگی وضعیت |
| مبلغ کل | جمع آیتم‌ها |
| تاریخ | تاریخ انتشار |

### صفحهٔ ویرایش

- **اطلاعات پیش‌فاکتور** — دادهٔ مشتری و خط محصول (با RTL در صورت فعال بودن `rasam-server-config`)
- **پیگیری تماس** — تیک «تماس گرفته شد» + فیلد گزارش
- **مدیریت PDF** — عملیات فایل

---

## ذخیره‌سازی PDF و دیتابیس

### مسیر فایل‌ها

```
wp-content/uploads/quotations/quotation-{post_id}.pdf
```

### متای مسیر PDF

- کلید: `rq_pdf_path` — مسیر کامل فایل روی سرور

### حذف خودکار (نسخه 2.4.0+)

| رویداد | رفتار |
|--------|--------|
| **گذشت ۶۰ روز (دو ماه)** از تاریخ درخواست | Cron روزانه فقط **فایل PDF** را حذف می‌کند؛ پست و متای مشتری/محصول در دیتابیس می‌ماند |
| حذف درخواست (پست) | فایل PDF همراه پست حذف می‌شود (`before_delete_post`) |
| حذف دستی از ادمین | دکمه «حذف PDF» در متاباکس |
| **ساخت مجدد** | دکمه «ساخت مجدد PDF» — از همان دادهٔ ذخیره‌شده در پست، فایل دوباره ساخته می‌شود |

ثابت پیش‌فرض: `WC_RQ_PDF_RETENTION_DAYS` = `60`. قابل تغییر با فیلتر:

```php
add_filter( 'wc_rq_pdf_retention_days', function () {
    return 90; // مثال: سه ماه
} );
```

رویداد Cron: `wc_rq_cleanup_old_pdfs` (روزانه).

### امنیت دسترسی PDF

- URL فایل‌ها از طریق `uploads/quotations/` در دسترس است اگر مسیر حدس زده شود.
- پیشنهاد آینده: دانلود از طریق endpoint ادمین با capability یا نام فایل تصادفی.

---

## ساختار فایل‌ها

```
wc-request-quotation/
├── wc-request-quotation.php      # bootstrap اصلی + کلاس WC_Request_Quotation
├── README.md                     # همین فایل
├── .editorconfig                 # UTF-8 بدون BOM
├── assets/
│   ├── style.css                 # استایل پاپ‌آپ فرانت
│   ├── script.js                 # AJAX فرانت
│   ├── admin-support.css         # لیست و متاباکس تماس ادمین
│   └── admin-support.js          # نمایش فیلد گزارش با تیک تماس
├── includes/
│   ├── wc-rq-loader.php          # بارگذاری امن PHP بدون BOM
│   └── class-wc-rq-support.php   # تماس، لیست رنگی، ویجت پیشخوان
├── fonts/                        # فونت‌های PDF (IRANYekan)
├── assets/img/                   # لوگو PDF
└── lib/tcpdf/                    # کتابخانه TCPDF (bundled)
```

---

## متا فیلدها

| کلید | نوع | توضیح |
|------|-----|--------|
| `rq_name` | string | نام مشتری |
| `rq_phone` | string | شماره تماس |
| `rq_company` | string | شرکت |
| `rq_national_id` | string | کد ملی / اقتصادی |
| `rq_items` | array | `[{ id, name, qty, price, total }]` |
| `rq_is_variable` | int | 1 = محصول متغیر (ستون کانفیگ در PDF) |
| `rq_pdf_path` | string | مسیر فایل PDF |
| `rq_needs_support` | int | درخواست تماس |
| `rq_support_seen` | int | ادمین لیست/جزئیات را دیده |
| `rq_support_called` | int | تماس گرفته شده |
| `rq_support_report` | text | گزارش مختصر تماس |

---

## یکپارچگی با Rasam Server Config

افزونه `rasam-server-config`:

- متاباکس پیش‌فرض `rq_meta_box` را با نسخهٔ RTL (`rsc_rq_meta_box`) جایگزین می‌کند
- جزئیات وریشن و قطعات اضافه را پارس می‌کند

افزونه پیش‌فاکتور **همیشه** متاباکس `rq_meta_box` را ثبت می‌کند؛ اگر RSC فعال نباشد، همان نسخهٔ پایه نمایش داده می‌شود.

---

## عیب‌یابی

### پیام «۳ کاراکتر غیرمنتظره» هنگام فعال‌سازی

**علت:** فایل PHP با **UTF-8 BOM** ذخیره شده (معمولاً `includes/class-wc-rq-support.php`).

**راه‌حل:**

1. در ویرایشگر encoding را **UTF-8 without BOM** کنید
2. از نسخهٔ 2.2.3+ لودر `wc_rq_load_include_file` BOM را حذف می‌کند
3. افزونه را غیرفعال و دوباره فعال کنید

### AJAX «ارسال ناموفق» یا خرابی AJAX سایر افزونه‌ها

همان علت BOM — خروجی قبل از JSON. پس از رفع BOM و پاک کردن کش، باید برطرف شود.

### قیمت PDF در سفارشی‌سازی کمتر از جمع مودال RSC

**علت:** قیمت نهایی مودال (پایه + قطعات) به‌اشتباه مثل `data-price` جدول وریشن `/10` می‌شد.

**راه‌حل (2.4.3+):** فقط افزونهٔ `wc-request-quotation` — فیلد مخفی `wc-rq-price-from-vartable`؛ `/10` فقط وقتی از دکمهٔ `.wc-rq-btn` آمده باشد. **نیازی به تغییر `rasam-server-config` نیست.** کش را پاک کنید.

### استایل لیست ادمین (badge / رنگ ردیف) نمایش داده نمی‌شود

- کش مرورگر و WP Rocket را پاک کنید
- بررسی کنید `assets/admin-support.css` لود می‌شود (نسخه در query string)

### فیلد گزارش تماس فقط بعد از ذخیره باز می‌شود

- `assets/admin-support.js` باید در صفحهٔ ویرایش لود شود
- با تیک زدن «تماس گرفته شد» باید بلافاصله باز شود (بدون ذخیره)

---

## توسعه و نگهداری

### هوک‌های AJAX

| action | دسترسی |
|--------|--------|
| `wc_rq_submit_form` | مهمان + کاربر |

### ثابت‌ها

```php
WC_RQ_PLUGIN_FILE           // مسیر فایل اصلی افزونه
WC_RQ_PDF_RETENTION_DAYS    // مدت نگهداری PDF (پیش‌فرض 60)
WC_RQ_DOCS_URL              // لینک دانشنامه در GitHub
```

### بارگذاری فرانت

| محل | CSS/JS پاپ‌آپ | assetهای ادمین |
|-----|----------------|----------------|
| صفحهٔ تک‌محصول (`is_product()`) | بله | — |
| سایر صفحات فرانت | خیر | — |
| پنل ادمین | — | بله (لیست/ویرایش `quotation_request`) |

Handleهای `wc-rq-style` و `wc-rq-script` در صفحهٔ محصول **ثبت** می‌شوند تا افزونهٔ `rasam-server-config` بتواند به آن‌ها وابسته شود.

### کلاس‌ها

- `WC_Request_Quotation` — CPT، PDF، فرم، تنظیمات
- `WC_RQ_Support` — تماس و UI ادمین

### نکات کدنویسی

- فایل‌های `includes/*.php` را **فقط** با UTF-8 بدون BOM ذخیره کنید
- پس از تغییر CSS/JS نسخه enqueue را در `wc-request-quotation.php` افزایش دهید
- TCPDF سنگین است؛ فقط هنگام `generate_pdf_invoice` لود می‌شود

### تست دستی پیش از انتشار

- [ ] محصول ساده — PDF بدون ستون کانفیگ
- [ ] محصول متغیر از جدول — PDF با کانفیگ
- [ ] چک‌باکس تماس + لیست ادمین
- [ ] ساخت مجدد / حذف PDF از ادمین
- [ ] فعال‌سازی افزونه بدون هشدار BOM

---

## تاریخچه نسخه‌ها

### 2.4.4

- رفع قیمت **محصول ساده**: `variation_id` مقدار `"0"` دیگر به‌اشتباه وریشن حساب نمی‌شود
- `price_from_vartable` فقط وقتی قیمت از **ردیف جدول وریشن** + وریشن معتبر باشد

### 2.4.3

- رفع قیمت سفارشی‌سازی **فقط در `wc-request-quotation`** (`price_from_vartable`) — بدون ویرایش RSC
- ریست فلگ قیمت هنگام کلیک «دریافت پیش‌فاکتور» در مودال (کلاس `.rsc-vt-modal__quotation`)

### 2.4.2

- رفع باگ قیمت PDF در سفارشی‌سازی (نسخهٔ میانی؛ در 2.4.3 به‌جای وابستگی به RSC، منطق داخل همین افزونه است)
- لینک تلگرام نویسنده: [@amir_drwp](https://t.me/amir_drwp)

### 2.4.1

- بارگذاری `style.css` / `script.js` و HTML پاپ‌آپ **فقط** در صفحهٔ تک‌محصول ووکامرس
- حذف دکمه پیش‌فاکتور از حلقهٔ آرشیو فروشگاه (`shop loop`)
- لینک **دانشنامه** در صفحهٔ افزونه‌ها (`plugin_row_meta`) → [مستندات GitHub](https://github.com/AmirheydaripourOdin1133/plugins-document/tree/main/wc-request-quotation)

### 2.4.0

- حذف خودکار PDFهای قدیمی‌تر از **۶۰ روز** (فقط فایل؛ رکورد درخواست باقی می‌ماند)
- Cron روزانه `wc_rq_cleanup_old_pdfs` + پاک‌سازی فایل‌های یتیم در `uploads/quotations`
- ساخت مجدد PDF از ادمین پس از حذف خودکار یا دستی

### 2.3.0

- مستندات کامل (`README.md`)
- به‌روزرسانی هدر افزونه و نویسنده
- بدون تغییر منطق اصلی نسبت به 2.2.4

### 2.2.4

- رفع مسیر اشتباه CSS/JS ادمین (لیست رنگی و badge تماس)
- لود اجباری assetهای ادمین از فایل اصلی
- بهبود نمایش فیلد گزارش تماس قبل از ذخیره

### 2.2.3

- لودر امن `wc-rq-loader.php` برای جلوگیری از خرابی AJAX به‌خاطر BOM
- ارسال `clean_price` در AJAX
- try/catch ساخت PDF

### 2.2.2

- بازگردانی متاباکس «اطلاعات پیش‌فاکتور»
- ثبت `RSC_Quotation_Admin` مستقل از وابستگی‌های RSC

### 2.2.0

- ستون کانفیگ فقط برای محصول متغیر
- گردش‌کار تماس/پشتیبانی (فرانت + ادمین + ویجت پیشخوان)
- تنظیمات متن چک‌باکس تماس

### نسخه‌های قدیمی‌تر

- تولید PDF فارسی با TCPDF
- فرم پاپ‌آپ و CPT `quotation_request`

---

## کارهای پیشنهادی آینده

اولویت‌بندی پیشنهادی برای توسعهٔ بعدی (هنوز پیاده‌سازی نشده):

| اولویت | موضوع |
|--------|--------|
| متوسط | تنظیم تعداد روز نگهداری PDF در صفحهٔ تنظیمات (الان ثابت ۶۰ روز + فیلتر) |
| بالا | **nonce** برای لینک‌های regenerate/delete PDF در ادمین |
| متوسط | بازسازی ساختار: جدا کردن PDF، Admin، Ajax در فایل‌های مجزا |
| متوسط | دانلود PDF با capability به‌جای URL مستقیم uploads |
| پایین | Rate limit روی `wc_rq_submit_form` |
| پایین | `uninstall.php` برای پاک‌سازی optionها هنگام حذف افزونه |

---

## مجوز

افزونه اختصاصی رسام سرور — استفاده داخلی. برای استفاده/توزیع خارجی با نویسنده هماهنگ کنید.

---

**نگهدارنده:** [Amir heydaripour](https://t.me/amir_drwp) — تلگرام: [@amir_drwp](https://t.me/amir_drwp)
